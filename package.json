{
  "private": true,
  "scripts": {
    "format": "elm-format src tests",
    "start": "elm-doc-preview --port=8020",
    "review": "elm-review --fix",
    "test": "elm-review && elm-test && elm-format --validate src tests && elm make --docs=/tmp/docs.json"
  },
  "devDependencies": {
    "elm-doc-preview": "^5.0.3",
    "elm-format": "^0.8.3",
    "elm-test": "^0.19.1"
  },
  "dependencies": {
    "elm": "^0.19.1-5",
    "elm-review": "^2.6.1",
    "rimraf": "^2.7.1"
  }
}
